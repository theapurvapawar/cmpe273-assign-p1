/*
var ecommerceApp = angular.module('ecommerce-app', [
'ecommerceControllers'
]);
*/
/*
ecommerceApp.config(['$routeProvider', '$locationProvider',
  function($routeProvider) {
    $routeProvider.
      when('/store', {
        templateUrl: 'partial/store',
        controller: 'StoreController'
      }).
      when('/store/tv', {
        templateUrl: 'includes/category',
        controller: 'CategoryController'
      }).
      otherwise({
        redirectTo: '/store'
      });

      $locationProvider.html5mode(true);
  }]);
*/